load("@rules_rust//rust:defs.bzl", "rust_library")

exports_files(["Cargo.toml"])

rust_library(
    name = "portable_eval",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "portable_eval",
    edition = "2024",
    visibility = ["//visibility:public"],
    deps = [
        "//crates/check:portable_check",
        "//crates/ir:portable_ir",
    ],
)
