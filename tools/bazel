#!/usr/bin/env bash
set -euo pipefail
readonly root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONDONTWRITEBYTECODE=1
exec python3 "${root}/tools/dev/storage_guard.py" "${BAZEL_REAL:?Run through Bazelisk}" "$@"
