load("@rules_rust//rust:defs.bzl", "rust_library")

exports_files(["Cargo.toml"])

rust_library(
    name = "portable_diagnostics",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "portable_diagnostics",
    edition = "2024",
    visibility = ["//visibility:public"],
)
